# entropix-engine
Zero-GC browser game engine in vanilla JS. Fixed 60 TPS logic. Structure-of-Arrays entity pools, and a linked-list  spatial hash that cuts 3,000-entity collision checks from O(N²) to  O(N×K). No frameworks. No dependencies. Read the source.
